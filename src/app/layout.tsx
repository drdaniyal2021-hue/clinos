import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ClinOS — Clinical Operating System",
  description:
    "AI-guided step-by-step clinical workflow system for junior doctors. From ER triage to discharge documentation.",
  keywords: ["clinical", "emergency medicine", "medical AI", "ER workflow", "junior doctors"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      // "dark" class = dark mode active by default (ClinOS default)
      // Remove "dark" and add "light" class to switch themes via JS toggle
      className={`${geistSans.variable} ${geistMono.variable} dark h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
